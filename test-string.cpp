#include <string>
#include <vector>
#include <iostream>
using namespace std;
int main(int argc, char const *argv[]) {
    string s{}, t{};
    // if (s[12] == t[13]) cout << 1 << endl;
    if (s[0] == t[0] && s[1] == t[1]) cout << 1 << endl;
    return 0;
}
